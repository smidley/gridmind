# GridMind Backend Configuration (for local development)
# Most settings are configured through the web UI at http://localhost:8080/settings

# Optional overrides
GRIDMIND_POLL_INTERVAL_SECONDS=30
GRIDMIND_DEBUG=true
